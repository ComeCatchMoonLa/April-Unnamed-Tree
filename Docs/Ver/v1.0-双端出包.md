# 1.0 双端出包

- 目标：出包前补表·四人称、里·三展示肢、中章留屏；然后 Windows 与 Android 脱离编辑器可运行；全量验收通过。
- 前置：0.9
- 标签：`v1.0.0`
- 必读：[验收清单](../Dev/测试/验收清单.md)、[游戏设计-存档与播放](../Dev/设计/游戏设计-存档与播放.md) §3.2 §4、[接口设计-播放与UI](../Dev/设计/接口设计-播放与UI.md) §4 §6、[测试策略](../Dev/测试/测试策略.md)、[GitHub展示](../Dev/Git/GitHub展示.md)
- 做法：一次只做下面一段；该段勾完并测过，再开下一段。不要把三处修正和出包揉进一次改动。

共用约束（每段都遵守）：

- 不要改节点 ID。不要新增第三种 style、第四类选择肢。
- 不要改 `run_choice_display` 总入口来做留屏。中章用 `scr_choice_hold`；里·三仍走 modal 展示肢。
- 本版本不做：语义黑、Staff 散场暗、选项跟表里色、标题日历/「她的频道」、系统亮度、花瓣/转场、里·三长期留屏。

## 第 1 段：设计与 TC

已有：0.9 可通读；设计师已拍板（里·三不留屏；TC10 改成空白推进、按灭再收；中章两行 `text`）。

- [x] 存档与播放 §3.2：中章留屏外观；里·三关掉再读
- [x] 存档与播放 §4：`ch3_black` 走 `ura`；空 `who` /「他」
- [x] TC10、TC16、TC26；接口 `show_choice_hold` / `scr_choice_hold`
- [x] 把这三刀写进出包 TODO 之前

本段不做：游戏代码、出包。

测：对照设计评审清单无冲突。节点 ID 未改。

## 第 2 段：表·四人称 + 里·三展示肢

已有：第 1 段文档。

- [ ] `ch3_black`：`apply_style("ura")`；`_style_for_node` 对 `ch3_black` 返回 `ura`
- [ ] 「还在？」空 `who`；「在。」用 `CHAR_HE`
- [ ] 里·三「……」「……」改 `run_choice_display`；Log「选项（未选择）」
- 不要改中章、不要动 `run_choice_display` 的关闭语义

本段不做：中章留屏、出包。

测：TC16（表·四）、TC26。首章假选项、真选项不变。

## 第 3 段：中章两行 `text` 同屏

已有：第 2 段。

- [ ] `scr_choice_hold`：两行 `text`，无底、无悬停、不吃点击；非 modal；zorder 低于 HUD 与对白
- [ ] `show_choice_hold` / `hide_choice_hold`；Log 只在第一次亮出时记「选项（未选择）」
- [ ] `ch2_choice:0001` 之后亮出；`0002`–`0005` 句 label 保证已亮；`0006` 先收再播「按灭屏幕」
- [ ] 不要调用 `run_choice_display`。`commit_leave` / 清会话必须收掉。HUD 不把 hold 算进 overlay 遮挡
- 不要改首章/真选项、不要改里·三

本段不做：出包。

测：TC10。点在两行字上也能翻句；中途 Return 再续还在。

## 第 4 段：出包

已有：第 1–3 段。

- [ ] `renpy lint` 出包前无未记录错误（TC25）
- [ ] Windows 构建，脱离 Launcher 运行（TC01）
- [ ] Android APK/AAB：恒定全屏；隐藏窗口/全屏设置行；返回键 = Return（TC23）
- [ ] 无网页构建物
- [ ] 按验收清单 TC01–TC26 全勾（NA 须写原因）
- [ ] 根 README 按 GitHub 展示模板落盘（License 仍待填则保持待填）
- [ ] Release 资产命名：`April-Unnamed-Tree-win-v1.0.0.zip` / `...-android-v1.0.0.apk`
- [ ] 可选：白底截图两张进 `Docs/Res/screenshots/`

## 完成（四段都过）

- 表·四人称、里·三展示肢、中章留屏已按契约
- Win 与 Android 均可离线运行
- 验收清单全部 Pass 或有记录的 NA
- Git 标签 `v1.0.0`
