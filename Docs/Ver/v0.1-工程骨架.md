# 0.1 工程骨架

- 目标：用 Ren'Py 8 打开本仓库能进游戏并见到白屏；开发环境按文档落地。
- 前置：设计评审通过。
- 标签：`v0.1.0`
- 必读：[开发环境](../Dev/调研/开发环境.md)、[架构设计](../Dev/设计/架构设计.md)、[Git工作流](../Dev/Git/Git工作流.md)、[编码规范](../Dev/实现/编码规范.md)

## TODO

- [ ] 用 Ren'Py 8 Launcher 把 `Sakura/` 建成工程（不要用系统 Python 跑）
- [ ] 按开发环境拷贝 `.vscode/extensions.json` 与 `settings.json`
- [ ] 安装并启用 `renpy.language-renpy`；`.rpy` 语言模式为 `renpy`
- [ ] 按 Git 工作流写入根 `.gitignore`
- [ ] 创建 `game/images/white.png`（纯白）；`bg_*` / `cg_*` 可暂后放到 0.2
- [ ] 放入两套随包字体到 `game/fonts/`（Noto Sans SC / Noto Serif SC 或同许可替代）
- [ ] `engine_flags`：关 rollback、Skip、Hide、退出确认、默认存档柜；**打开** auto-forward
- [ ] 瘦身默认 `gui`/`options`，禁止巨型未拆文件
- [ ] `boot` 启动后进入白底占位（尚无完整标题也可）
- [ ] 短 `.cursor/rules` 指针（只引用 `Docs/Dev/README.md` 与本目录，不粘贴长文）

## 完成

- Launcher 能启动，无报错白屏
- 滚轮/右键不能回滚；无退出确认
- `renpy lint` 能跑（允许尚无剧本的空工程警告，需在记录里写明）
- 本版本不测玩法 TC
