# 0.4 播放会话与 Auto

- 目标：`PlaySession` 能新的游戏 / 读槽跳转；Auto 为引擎默认 AFM。
- 前置：0.3
- 标签：`v0.4.0`
- 必读：[接口设计-播放与UI](../Dev/设计/接口设计-播放与UI.md) §1–3、[游戏设计-存档与播放](../Dev/设计/游戏设计-存档与播放.md) §2

## TODO

- [ ] `PlayContext`（无 pending/active 字段）
- [ ] `start_new_game`：覆盖槽为 `ch1_start` 首句，无确认
- [ ] `continue_game`：无槽 False；有槽从 `line_id` **开头**播
- [ ] `on_node_reached` / `on_cg_shown` / `on_line_shown` / `commit_leave`
- [ ] `replay` 下所有写槽、unlock 早退
- [ ] `jump_to_line(line_id)` 骨架（可先只支持已有占位 label）
- [ ] `toggle_afm`：立刻切 `preferences.afm_enable`
- [ ] `SettingsStore.sync_afm` 接到三档 `afm_time`
- [ ] **禁止** `auto_controller.rpy` 与点击不退出逻辑
- [ ] 打字 CPS = 50

## 完成

- 可用最小测试句验证：新的游戏覆盖、杀进程后从句首继续
- Auto：不点会自动前进（TC18）；点击行为与引擎默认一致（TC19）
- 对照 TC03、TC04（可用占位文本）
