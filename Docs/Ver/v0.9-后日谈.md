# 0.9 后日谈与里后日谈

- 目标：`after` / `inner` 全文；读完后日谈长出里；里线变暗；§3.4 入口续玩。
- 前置：0.8
- 标签：`v0.9.0`（两段都勾完再打）
- 必读：[Docs/Res/剧本.txt](../Res/剧本.txt)（只读，禁止改）、[游戏设计-存档与播放](../Dev/设计/游戏设计-存档与播放.md) §3.3–5、[游戏设计-产品与叙事](../Dev/设计/游戏设计-产品与叙事.md) after/inner 节点、[接口实现约定](../Dev/实现/接口实现约定.md) §1.4
- 做法：一次只做下面一段；该段勾完并测过，再开下一段。不要把 after / inner 揉进一次改动。

共用约束（每段都遵守，不必每条再抄）：

- 每句稳定 `line_id`：`{node_id}:{四位}`；只追加不重排。`jump_to_line` 必须能落到**本段已写**的任意句。
- 到达产品表节点调用 `on_node_reached`。无新 CG。
- `【BGM】` / `【SE】` 只留标记，不播放。场景全指向 `white`。不要做「黑屏两秒」转场。
- 单文件 ≤500 行，超了按节点拆；`game/script/`（或章子目录）≤15 个文件。
- 回放从点入节点播到本章连续已解锁的末句，不写槽、不解锁。play 章末回标题，不自动进另一章。
- 不要改节点 ID。不要采用剧本「通关两次才出后日谈」。
- 标题主键换字、真选项去向已在 0.8；`enter_after` / `enter_inner` 未解锁返回 False 已有。

## 第 1 段：后日谈

已有：真选项解锁 after；标题点「后日谈」进 `after_start` 占位；`enter_after` 总是从章首进。

节点：`after_start` → `after_read` → `after_night` → `after_tree`。

- [ ] `game/script/after.rpy`：按剧本后日谈逐句落地；占位从 `jump.rpy` 挪走（超 500 行则 `game/script/after/` 按节点拆）
- [ ] 引号对白按「她说 / 他说」标说话人；旁白无 `who`
- [ ] play：`after_tree` 段落后 `unlock_inner`，回标题；主键仍是「后日谈」，正下方长出「里·后日谈」
- [ ] `enter_after`：槽已在 after 则从该 `line_id` 句首续，否则进 `after_start`
- [ ] replay：从本章已解锁节点播到连续已解锁末句，不写槽、不 `unlock_inner`

本段不做：inner 全文、里线变暗验收、`config.version`、打 `v0.9.0`。长出的「里·后日谈」仍可进现有占位。

测：标题点「后日谈」可读完（无新 CG）。中途 Return 再点从离开句开头。读完回标题后 TC13。TC11/TC12 点「后日谈」进正式首句。鉴赏 after 节点不脏槽。

## 第 2 段：里后日谈

已有：第 1 段；标题已能长出「里·后日谈」；`inner_start` 占位；`sync_inner_dim` 与 inner → `ura` 已有。

节点：`inner_start` → `inner_mail` → `inner_note` → `inner_night` → `inner_slope` → `inner_end`。  
`▎里·六` 无独立节点，接到 `inner_end`。

- [ ] `game/script/inner.rpy`：按剧本里后日谈逐句落地；换掉占位（超限按节点拆）
- [ ] 旁白为主；需要时 `who` 为「他」
- [ ] `enter_inner`：槽已在 inner 则从该 `line_id` 句首续，否则进 `inner_start`
- [ ] 进 inner（游玩或回放）立刻 `sync_inner_dim`；Log/设置保持；回标题立刻恢复
- [ ] 双通关后点「里·后日谈」停在末句，不循环回正篇（§3.4）
- [ ] `config.version` → `0.9.0`
- [ ] Launcher `renpy lint`

本段不做：Win/Android 出包（1.0）。

测：点「里·后日谈」可读完。TC14、TC15。中途 Return 再点从离开句开头。鉴赏 inner 不脏槽。本段最后 lint。

## 完成（两段都过）

- 从标题可读完 after，再可读完 inner
- TC13、TC14、TC15
- TC11/TC12 进正式 after 首句
- 标签 `v0.9.0`
