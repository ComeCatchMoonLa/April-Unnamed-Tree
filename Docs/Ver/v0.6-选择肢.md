# 0.6 选择肢

- 目标：三种选择肢 screen 行为正确。可用占位章节接入，不必等全文。
- 前置：0.5
- 标签：`v0.6.0`
- 必读：[游戏设计-存档与播放](../Dev/设计/游戏设计-存档与播放.md) §3、[接口设计-播放与UI](../Dev/设计/接口设计-播放与UI.md) §4

## TODO

- [x] `run_choice_fake` + `scr_choice_fake`：两可点按钮，殊途同归，无分支变量；Log「选择：」+ 文案
- [x] `run_choice_display` + `scr_choice_display`：不可点、无悬停；空白推进；Log「选项（未选择）」
- [x] `run_choice_true` + `scr_choice_true`：继续 / 结束；两者 `unlock_after`
- [x] 「继续」进 `after_start`（after 脚本可 0.9 再填，本版本可占位 label）
- [x] 「结束」写 after 首句后回标题，无确认
- [x] 三个独立 screen，禁止一个 screen 用大量 `if` 混三种交互
- [x] 选择肢不是 `line_id`

## 完成

- TC09、TC10、TC11、TC12（after 正文可仍是占位）
