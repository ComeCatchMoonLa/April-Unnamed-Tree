# 0.2 数据目录

- 目标：节点与 CG 表进 `game/data/`，`NodeCatalog` 可查。
- 前置：0.1
- 标签：`v0.2.0`
- 必读：[游戏设计-产品与叙事](../Dev/设计/游戏设计-产品与叙事.md)、[接口设计-数据与存档](../Dev/设计/接口设计-数据与存档.md) §2–3、[接口实现约定](../Dev/实现/接口实现约定.md) §1.1

## TODO

- [x] `game/data/nodes.rpy`：29 个节点，ID 与设计表逐字一致
- [x] `game/data/cgs.rpy`：四个 CG ID
- [x] `game/data/copy.rpy`：快捷键说明、Staff 文案、选择肢文案可先放常量
- [x] `game/system/catalog.rpy`：实现 `get_node` / `nodes_in_chapter` / `all_nodes` / `chapter_of` / `first_line_id` / `chapter_last_line_id` / `get_cg` / `all_cgs` / `route_of_chapter`
- [x] `image`：所有 `bg_*` `cg_*` 指向 `white`
- [x] 未知 ID 返回 `None`，不抛给 UI
- [x] `game/data/` 与 `game/system/` 各自 ≤15 个文件

## 完成

- 可在控制台或临时调试句打印 29 节点与 4 CG
- ID 禁止改名
- 无存档/无 UI 也可结束本版本

## 验收

- 启动仍进白屏。控制台 `jump catalog_probe` 应显示 `29 节点 / 4 CG；未知 ID 为 None：True`
- `log.txt` 的 NodeCatalog 行应列出 29 个 `node_id` 与 4 个 `cg_id`

## lint 记录（2026-09-15 20:56:16）

Launcher Ren'Py 8.5.3.26051504，无报错、无警告。

- 1 dialogue block（`catalog_probe` 那一句）
- 0 menus, 1 images, 10 screens

`renpy.image()` 注册的 `bg_*` / `cg_*` 不计入 lint 的 images 统计，只算磁盘上的 `white.png`。空工程无章对白，不阻断 0.2。
