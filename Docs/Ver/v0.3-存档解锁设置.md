# 0.3 存档、解锁、设置

- 目标：单槽 `"0"`、全局解锁、四项设置可读写；UI 仍可没有。
- 前置：0.2
- 标签：`v0.3.0`
- 必读：[游戏设计-存档与播放](../Dev/设计/游戏设计-存档与播放.md) §1、[接口设计-数据与存档](../Dev/设计/接口设计-数据与存档.md) §4–6

## TODO

- [ ] `SaveStore`：`has_slot` / `load_slot` / `write_record` / `write_line`
- [ ] 只暴露 `slot_id = "0"`；`play_mode != play` 拒绝写入
- [ ] `reserved` 写入时强制 `{}`
- [ ] `UnlockStore`：节点/CG/after/inner，幂等，放 `persistent` 不放槽
- [ ] `SettingsStore`：`afm_level` `font_size_level` `font_id` `window_mode`；`sync_afm`；`text_cps` 恒 50
- [ ] Android 忽略 `window_mode`
- [ ] UI 与脚本仍不得直接 `renpy.save` / 改 `persistent.nodes`
- [ ] 损坏槽 `load_slot` 返回 `None`

## 完成

- 可用临时调用写入槽 `"0"` 后重启仍能读到同一 `line_id`
- 解锁关游戏仍在
- 相关：为 TC04/TC05 打基础，本版本可用最小调试入口验证读写
