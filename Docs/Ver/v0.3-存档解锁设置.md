# 0.3 存档、解锁、设置

- 目标：单槽 `"0"`、全局解锁、四项设置可读写；UI 仍可没有。
- 前置：0.2
- 标签：`v0.3.0`
- 必读：[游戏设计-存档与播放](../Dev/设计/游戏设计-存档与播放.md) §1、[接口设计-数据与存档](../Dev/设计/接口设计-数据与存档.md) §4–6

## TODO

- [x] `SaveStore`：`has_slot` / `load_slot` / `write_record` / `write_line`
- [x] 只暴露 `slot_id = "0"`；`play_mode != play` 拒绝写入
- [x] `reserved` 写入时强制 `{}`
- [x] `UnlockStore`：节点/CG/after/inner，幂等，放 `persistent` 不放槽
- [x] `SettingsStore`：`afm_level` `font_size_level` `font_id` `window_mode`；`sync_afm`；`text_cps` 恒 50
- [x] Android 忽略 `window_mode`
- [x] UI 与脚本仍不得直接 `renpy.save` / 改 `persistent.nodes`
- [x] 损坏槽 `load_slot` 返回 `None`

## 完成

- 可用临时调用写入槽 `"0"` 后重启仍能读到同一 `line_id`
- 解锁关游戏仍在
- 相关：为 TC04/TC05 打基础，本版本可用最小调试入口验证读写

## 验收入口

控制台 `jump store_probe`：写入 `"0"` / 拒绝 replay / 解锁 / 保存设置。关游戏再开后 `jump store_probe_reload` 应仍显示 `ch1_start:0001`。`jump store_probe_damage` 应显示损坏槽为 None（测完恢复原槽）。

槽与解锁、设置分三个 persistent 字段；`load_slot` 不 `renpy.load` 整局。`afm` 三档秒数：慢 30 / 中 15 / 快 5。

`store_probe` 的「拒绝回放」打印的是 `write_record` 返回值；拒绝成功时为 `False`。

## lint 记录（2026-09-20 13:19:23）

Launcher Ren'Py 8.5.3.26051504，无报错、无警告。

- 6 dialogue blocks（`catalog_probe` 1 + `store_probe` 3 + `store_probe_reload` 1 + `store_probe_damage` 1）
- 0 menus, 1 images, 10 screens
