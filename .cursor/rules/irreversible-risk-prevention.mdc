---
description: Sakura 不可逆风险补充（出仓包装、兄弟工程、全局 Python）
alwaysApply: true
---

# Sakura 不可逆补充

全局标准仍然有效：不可逆本身不够格；只对难恢复且高损失、或影响范围容易失控的动作按第 0 档处理。本仓库额外这几类不得自动执行：

- 用 `python` / `py` 套一层，对当前 `Sakura` 工作区外做改写或删除。
- 对 `D:\Project\Renpy_Projects` 下、非本工作区的路径做清理、改写或删除（不要动其中其他工程）。
- 把依赖装进系统 Python 或用户全局环境（会污染仓外）。

运行游戏用 Ren'Py Launcher。不要把仓内改代码、只读检查、或用系统 Python 启动游戏当成同一档拦截。
